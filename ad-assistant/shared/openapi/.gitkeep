# OpenAPI specifications — the authoritative source for API contracts.
# Sprint-02 Task-08: first real spec created (mock-ai.yaml).
#
# Existing specs:
# - mock-ai.yaml  (Mock AI API: POST /api/v1/mock-ai/ad-copy) ← FIRST
#
# Reserved specs:
# - auth.yaml    (Auth API: login, refresh, logout)
# - device.yaml  (Device API: bind, current)
# - ocr.yaml     (OCR API: submit task, query result)
# - usage.yaml   (Usage API: submit events)
# - credit.yaml  (Credit API: balance query)
# - provider-log.yaml (Provider Call Log API)
#
# All responses follow unified structure:
# { success: boolean, data: object|null, error: object|null, request_id: string }

# 任务单模板

## 任务名称

填写任务名称。

## 状态

`MVP_REQUIRED` / `MVP_OPTIONAL` / `BACKLOG` / `FUTURE` / `BLOCKED`

## 背景

说明为什么要做。

## 用户目标

记录用户原始目标和 Codex 的保守拆解结论。

## 本次只开发什么

- 

## 本次不开发什么

- 

## 允许修改哪些文件

- 

## 禁止修改哪些文件

- 

## 本地环境和服务规则

- 本地启动服务和测试时，优先使用本机已安装应用，例如 PostgreSQL、Redis、缓存、OCR 服务等。
- 本机没有对应应用时，先安装本地应用。
- 不默认使用 Docker；只有 CI service container 或用户明确批准 Docker fallback 时才允许。

## 注释语言要求

- CC 新增或修改代码注释时必须使用中文。
- 第三方协议、外部 API 固定术语、错误码和英文专有名词除外。

## 验收标准

- 

## 测试方式

- 

## 是否允许新增依赖

不允许，除非本节明确写明依赖名称、用途和风险。

## 是否涉及重大变更

必须明确填写：是 / 否。

如果是，必须包含：
1. 修改原因
2. 风险点
3. 影响范围
4. 回滚方案
5. 是否兼容旧版本
6. 是否需要数据库迁移

## 安全检查

- 不下发 API Key 到客户端
- 不由客户端扣点
- 不由客户端决定套餐
- 不绕过云端授权
- 不明文保存 Token
- AI 调用写入 provider_call_log

## 回滚方案

-

## CC 必须暂停的情况

- 任务单缺字段、范围冲突或不可执行
- 需要修改禁止文件或 allowed files 之外的文件
- 测试失败且修复超出当前任务范围
- 发现 secrets、真实密钥、生产连接串或用户敏感数据泄露风险
- 需要 Docker 但未获用户明确批准
- 需要删除、清空或迁移用户数据
- 需要真实支付、真实 AI 调用或未授权 Provider

## 完成输出要求

执行者完成后必须输出：
- 修改文件列表
- 实现内容
- 未实现内容
- 测试命令和结果
- 风险点
- 是否触发重大变更
- 中文 commit message
- PR title/body 中文摘要
- 合并后的中文交付说明，包括用户确认来源、PR 编号、合并方式和合并结果
